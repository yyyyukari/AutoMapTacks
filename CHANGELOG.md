# 更新日志

## 正式版本 v60

- 修复关闭奇观逐地块诊断时，大图书馆、牛津大学等需要相邻区域或改良的奇观可能访问空诊断对象并报错的问题。
- 多座奇观现在可以共享同一城市、同一地块上的同一支撑区域或改良；共享项目只计入一次占地、人口槽位、评分和最终放置。
- 修复区域候选可能落在已显示的奢侈或战略资源上的问题；普通规划与高加成分析现在共用同一项资源禁放校验，且无效的“收获资源”规划钉不能绕过该限制。
- 补齐油井与海上油井的资源和地形联合校验：必须同时存在已揭示石油并满足对应陆地或海洋地形。
- 优化自动规划中的收益计算与缓存：仅在影响范围允许时复用局部收益，并将运行期收益缓存限制为 4096 项，避免低命中率的完整方案缓存持续占用内存。
- 通过游戏当前生效的修改器及供电、宜居度、住房、间谍、忠诚度、宗教、人口、旅游业和贸易路线等状态依赖，判断收益是否必须动态重算；未知或 Mod 自定义规则仍采用保守回退。
- 代表性同城同设置测试中，平均预览等待时间由约 51.4 秒降至 28.4 秒，约减少 45%；规划期间临时内存增长约降低 36%。
- 未新增同分方案决胜规则；同分且等价的规划结果仍可能随运行环境或候选顺序变化。


## 正式版本 v59

- 正式版模组列表元数据改用全新的 `LOC_AMT_V59_MOD_*` 命名空间，避开创意工坊旧条目和客户端对 `LOC_AMT_MOD_*` 的缓存或冲突。
- 名称、简介和说明继续在 `.modinfo` 中为全部受支持语言内嵌注册；简体中文显示“自动地图钉规划”，说明末尾标记 `[v59]`。
- 功能代码与 v58 相同；本次只修复发布包名称及说明的显示链路。

## 正式版本 v58

- 修复“额外内容”模组列表把名称、简介和说明显示成 `LOC_AMT_MOD_*` 原始键名的问题。
- 模组浏览器元数据改为 Civ 6 实际支持的 `.modinfo` 内嵌本地化结构；英文、简体中文、繁体中文及其余受支持语言均可在列表阶段解析。
- 功能代码与 v57 相同；本次仅修复发布元数据、版本标识与对应的发布校验。

## 正式版本 v57

- 奇观候选继续采用数据库规则、DMT 放置检查和完整临时布局联合判断；相邻区域、改良设施及文明特色替代区域均走通用流程，不新增鲁尔山谷等单独特例。
- 自动规划与三环内手动地图钉可以共同提供奇观所需的相邻支撑；玩家未选择必要区域时仍会先给出醒目提示。
- 区域图标现在支持直接单击选择，同时保留拖动到优先槽位的用法；重复的未放置报告项和两条无实际帮助的界面提示已清理。
- 正式包恢复本地化名称、简介和说明的解析链路；简体中文、繁体中文及其他受支持语言均有可识别的模组列表元数据，说明末尾统一标记 `[v57]`。
- 面向测试的奇观逐地块诊断信息默认关闭，正式标题与加载日志不再带有 TEST／诊断版标识；发布校验会拒绝测试 UUID、旧版文本或遗漏前端本地化注册的包。

## 内部版本 v53

- 最终合法性统一交由 DMT 对完整的临时布局判定，不再以规划器自行复刻的简化奇观规则替代 DMT。
- 在安装模拟布局后刷新 DMT 地块缓存，并在退出模拟后再次清理，避免真实地图、上一候选和当前“区域＋奇观”组合相互污染。
- 未购买地块上的手动地图钉只会归属三环内最近的本方城市；固定工业区／特色替代区域可作为同城奇观的相邻锚点。
- 测试包、弹窗标题和加载日志更新为 `TEST v53`；模组作者名继续显示为 `yyyyukari`，Steam 上传账号为 `zvcxbnm123`。

## 内部版本 v52

- 修复奇观与相邻区域组合在最终评分阶段被 DMT 的实时地图钉缓存误判为非法的问题；规划器现在复用同等的地形、河流与资源规则，并直接对求解器中的完整组合复核相邻关系。
- 手动工业区／奥皮杜姆地图钉与自动成对规划使用同一套鲁尔山谷合法性判断，三环内满足条件的沿河地块不再因临时组合尚未写入地图而被丢弃。
- 保持未购买地块仅归属三格工作范围内最近城市的限制，不会把远处手动地图钉视为当前城市规划。

## 内部版本 v51

- 将奇观相邻区域/改良要求的读取、投影与同城复核移入主规划器，不再让候选生成依赖奇观辅助模块是否成功载入。
- 鲁尔山谷候选即使在尚无工业区地图钉时被 DMT 暂时报错，只要玩家明确同时选择工业区，仍会进入相邻组合生成；组合随后通过 DMT 覆盖模拟进行最终合法性裁决。
- 特色替代区域与标准区域在组合生成时统一按基础区域类型匹配，例如奥皮杜姆可稳定匹配工业区要求。

## 内部版本 v50

- 相邻区域提示改用独立的红色大号警告文本，不再与普通状态提示混在同一字体样式中。
- 玩家同时选择鲁尔山谷与工业区时，规划器现在先生成相邻组合，再通过与 DMT 相同的模拟地图钉覆盖执行整组合法性检查；只有 DMT 可接受的组合才进入候选列表。
- 已有手动工业区或奥皮杜姆地图钉仍作为固定锚点处理；联合规划不会另行生成重复区域。

## 内部版本 v49

- 奇观的相邻区域前置检查现在直接复用右侧专业区域槽位的锁定结果；当鲁尔山谷已勾选而工业区既未勾选、也未由本城已有区域或有效手动地图钉锁定时，会先提示玩家勾选工业区，不再直接进入求解并误报“没有合法地块”。
- 奇观选项在生成时保存数据库中的相邻区域要求，并保留直接读取 `Buildings.AdjacentDistrict` 的兜底，避免前置要求在不同数据读取路径间丢失。

## 内部版本 v48

- 同城手动专业区域钉现在会显示在右侧槽位中并锁定，同时继续遵守未购买地块仅归属工作范围内最近城市的限制。
- 依赖相邻区域的奇观会优先保留手动区域钉周围已经满足关系条件的候选；例如手动放置奥皮杜姆后，鲁尔山谷会在其相邻沿河合法格中求解。
- 已有手动区域钉时不再生成第二个自动配套区域；只有玩家明确同时选择区域和奇观时，算法才允许将两者作为新组合共同规划。

## 内部版本 v47

- 清除规划图标实例复用时残留的点击与拖拽回调，区域操作不再误勾选阿帕达那宫或任何其他奇观；奇观始终只能由玩家手动勾选。
- 依赖相邻区域的奇观不会反向改动玩家选择：例如只勾选鲁尔山谷时会提示先手动勾选工业区；两者都由玩家选择后，规划器才将奥皮杜姆与鲁尔山谷作为组合求解。

## 内部版本 v46

- 奇观相邻区域复核现在通过 `DistrictReplaces` 映射识别文明特色替代区域，例如将奥皮杜姆视为工业区。
- 位于尚未购买地块上的手动规划钉会归属到工作范围内最近的本方城市，可参与奇观的同城相邻条件判断。
- 更新测试版加载日志与界面版本标记，便于确认实际运行的是 v46。

## 内部版本 v45

- 测试版模组信息改用直接显示的 `TEST v45` 文本，不再受旧创意工坊版本同名本地化键覆盖。
- 本地安装测试副本使用独立模组 ID，与创意工坊正式版彻底分离，避免同 ID 多版本的加载优先级冲突。

## 内部版本 v44

- 在模组名称、简介、说明和游戏内规划窗口标题中显示测试版本号，便于确认实际加载的文件。
- 修正测试切换时使用了错误模组 ID 的问题；Auto Map Tacks 的实际 ID 为 `a3f7d2e1-9c4b-4e8a-bb13-7d11f9c2a04e`。

## 内部版本 v43

- 补齐全部 174 项繁体中文游戏内文本，繁中环境不再将规划器界面回退为英文。
- 缩短英文按钮上的高加成分析、特色区域和保护区文案，完整说明继续保留在悬浮提示中，避免文本超出按钮。
- 奇观本体及自动配套区域／改良现在会读取玩家预放置的清除地貌、移除资源等规划指令。

## 内部版本 v42

- 奇观候选现在会识别地形、河流、海岸、湖泊、资源以及相邻区域／改良等标准空间放置条件。
- 需要相邻区域或改良的奇观可与同城市的已有、已规划或自动配套项目组合求解；不新增奇观专用界面和通用报告提示。
- 已建成奇观的实时产出与魅力继续由游戏状态自动计入；勾选的未建奇观会额外模拟标准数据库 Modifier 中的地块产出、城市产出百分比、魅力和相邻加成。
- 使用标准 `Buildings`、`BuildingModifiers` 和要求集写法的模组奇观可自动受益于上述支持。

## 内部版本 v41

- 为德语、西班牙语、法语、意大利语、日语、韩语、波兰语、巴西葡萄牙语和俄语补充英文模组列表回退。
- Civ 6 官方支持的 12 种界面语言现在都会显示可识别的模组名称、简介和说明，不再裸露 `LOC_AMT_*` 键名。

## 内部版本 v40

- 修复繁体中文环境下模组列表直接显示 `LOC_AMT_MOD_NAME`、`LOC_AMT_MOD_TEASER` 和说明键名的问题。
- 为繁体中文模组列表补充名称、简介和说明；作者署名继续使用 `yyyyukari`。

## 1.0.0 Release Candidate（内部版本 v28）

- 支持区域、文明特色区域、改良设施、文明特色改良设施和奇观规划。
- 加入有顺序的专业区域槽位与可调规划人口。
- 改良设施数量改为按规划人口自动估算，并计入已有改良。
- 加入长期蓝图、按现状规划和自动补充勾选。
- 加入产出侧重点、完整地图预览和结构化规划报告。
- 报告单独列出所有未放置项目及原因。
- 支持已有地图钉、删除地貌、移除资源、种植树林等规划指令。
- 修复种植园等资源型改良设施出现在无效资源地块的问题。
- 修复专业区域人口限制、互斥区域、唯一项目和数量限制判断。
- 加入可取消的分阶段计算，避免长计算被误认为卡死。
- 优化中文界面、原版图标、数量控件和右侧报告布局。
- 发布版默认关闭详细搜索日志，只保留载入与错误信息。
- 新增屏幕左下方的独立“自动地图钉”入口按钮，同时保留 `Shift+P`。
- 所有主要规划偏好改为按城市分别记忆，不再让不同城市互相串用勾选项和专业区域顺序。
- 科技或市政完成时会使当前预览失效并刷新规则；重新预览会使用最新相邻加成。
- 模组作者署名改为 yyyyukari。
## 公开版 v40 元数据热修（2026-08-05）

- 以创意工坊已发布的 v39 为基线，不包含尚未发布的奇观规划修改。
- 为 `.modinfo` 补充前端元数据专用的 `LocalizedText` 文件注册，修复模组列表直接显示 `LOC_AMT_MOD_NAME` 等键名的问题。
- 保留现有英文与简体中文名称、简介和说明，只在两种语言的模组名称末尾增加 `[v40]` 标识。
